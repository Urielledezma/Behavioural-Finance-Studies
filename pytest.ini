[pytest]
# Each study carries its own tests/ beside its own src/, so pytest collects them
# all from studies/. importlib mode keeps two studies' conftest.py and test
# modules from colliding on name; each study's package still needs a unique name.
testpaths = studies
addopts = --import-mode=importlib
norecursedirs = .* _template results __pycache__
